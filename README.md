# backgammon-agents
 Created two agents to play backgammon, using an evaluation method to determine strong positions. One agent uses alpha-beta pruning on a minimax algorithm while the other one uses expectimax.
